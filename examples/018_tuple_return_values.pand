fn pair_values(x: i32) -> (i32, bool) {
    if x > 0 {
        return x, null
    }
    return (null, true)
}

(a, b) := pair_values(7)
print(a, b);

c, _ := pair_values(0)
print(c);

d, e: (i32, bool) = pair_values(-1)
print(d, e)
